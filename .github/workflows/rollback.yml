name: Deployment Rollback

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment to rollback'
        required: true
        type: choice
        options:
          - dev
          - stag
          - prod
      target-tag:
        description: 'Target Docker image tag / Git commit to rollback to'
        required: true
        type: string
      vault-url:
        description: 'URL of the Vault server'
        required: false
        type: string
        default: ''
      vault-role:
        description: 'Vault OIDC role'
        required: false
        type: string
        default: ''

jobs:
  get-ec2-ip:
    name: Get EC2 Public IP
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    outputs:
      ec2_ip: ${{ steps.terraform-output.outputs.ec2_ip }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target-tag }}

      - name: Fetch AWS Secrets from Vault
        if: ${{ inputs.vault-url != '' && inputs.vault-role != '' }}
        uses: ./.github/actions/vault-secrets
        with:
          vault-url: ${{ inputs.vault-url }}
          vault-role: ${{ inputs.vault-role }}
          vault-secrets: |
            secret/data/cloud_cost/aws access_key | AWS_ACCESS_KEY_ID ;
            secret/data/cloud_cost/aws secret_key | AWS_SECRET_ACCESS_KEY

      - name: Configure AWS Credentials
        uses: ./.github/actions/aws-auth
        with:
          aws-region: 'us-east-1'
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID || secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY || secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Initialize Terraform
        uses: ./.github/actions/terraform-init
        with:
          environment-directory: 'terraform/environment/${{ inputs.environment }}'

      - name: Get EC2 Public IP
        id: terraform-output
        shell: bash
        working-directory: terraform/environment/${{ inputs.environment }}
        run: |
          EC2_IP=$(terraform output -raw public_ip)
          echo "ec2_ip=${EC2_IP}" >> $GITHUB_OUTPUT
          echo "EC2 Public IP: ${EC2_IP}"

  rollback:
    name: Run Rollback
    needs: get-ec2-ip
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      packages: read
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.target-tag }}

      - name: Fetch Secrets from Vault
        if: ${{ inputs.vault-url != '' && inputs.vault-role != '' }}
        uses: ./.github/actions/vault-secrets
        with:
          vault-url: ${{ inputs.vault-url }}
          vault-role: ${{ inputs.vault-role }}
          vault-secrets: |
            secret/data/cloud_cost/ssh private_key | SSH_PRIVATE_KEY

      - name: Run Ansible Rollback
        uses: ./.github/actions/ansible-deploy
        with:
          ec2-host: ${{ needs.get-ec2-ip.outputs.ec2_ip }}
          ssh-private-key: ${{ env.SSH_PRIVATE_KEY }}
          image-tag: ${{ inputs.target-tag }}
          docker-repo: ${{ github.repository }}
          ghcr-username: ${{ github.actor }}
          ghcr-token: ${{ secrets.GITHUB_TOKEN }}
          playbook: 'rollback'
