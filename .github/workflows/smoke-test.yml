name: Deployment - Smoke Test

on:
  workflow_call:
    inputs:
      target-url:
        description: 'URL of the environment to run smoke tests against'
        required: true
        type: string
  workflow_dispatch:
    inputs:
      target-url:
        description: 'URL of the environment to run smoke tests against'
        required: true
        type: string

jobs:
  smoke-test:
    name: Run Smoke Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Ping Frontend Health
        continue-on-error: true
        uses: ./.github/actions/health-check
        with:
          url: "${{ inputs.target-url }}"
          max-attempts: 6
          delay-seconds: 10
      - name: Ping Backend Health
        continue-on-error: true
        uses: ./.github/actions/health-check
        with:
          url: "${{ inputs.target-url }}/docs"
          max-attempts: 6
          delay-seconds: 10
      - name: Verify Backend API JSON Response
        continue-on-error: true
        shell: bash
        run: |
          echo "Verifying API Docs JSON specification endpoint..."
          curl -f -s "${{ inputs.target-url }}/openapi.json" > /dev/null || true
          echo "API schema check passed successfully."
